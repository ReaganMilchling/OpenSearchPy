meta {
  name: model deploy
  type: http
  seq: 7
}

post {
  url: http://localhost:9200/_plugins/_ml/models/{{model_id}}/_deploy
  body: json
  auth: inherit
}

script:post-response {
  bru.setEnvVar("task_id", res.body.task_id);
}

settings {
  encodeUrl: true
}
