meta {
  name: tasks
  type: http
  seq: 6
}

get {
  url: http://localhost:9200/_plugins/_ml/tasks/{{task_id}}
  body: none
  auth: inherit
}

script:post-response {
  bru.setEnvVar("model_id", res.body.model_id);
}

settings {
  encodeUrl: true
}
